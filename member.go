package warrant

// Member is the representation of a group member resource within UAA.
// This is probably just a user.
type Member struct {
	// ID is the unique identifier for the member (e.g. user id).
	ID string
}
